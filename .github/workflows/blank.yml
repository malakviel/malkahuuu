name: Parar site no IIS via SSH

on:
  push:
    branches:
      - main

jobs:
  stop-iis-site:
    runs-on: ubuntu-latest
    steps:
      - name: Conectar ao servidor remoto via SSH
        uses: appleboy/ssh-action@master
        with:
           host: ${{ secrets.SFTP_SERVER }}
           username: marcelo.vasconcelos@vizcaya.com.br
           password: M09o092021*os
           port: 5051
           key: ${{ secrets.SSH_PRIVATE_KEY2 }}
           script: |
            iisreset /start "vizorder-desenvolvimento.vizcaya.com.br"
